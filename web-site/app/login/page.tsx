"use client";
import {useState} from 'react';
export default function Login(){const[error,setError]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const d=await r.json();if(!r.ok)throw Error(d.error);const next=new URLSearchParams(window.location.search).get('next')||'/';window.location.replace(/^\/hospitals\/[a-zA-Z0-9_-]+$/.test(next)?next:'/');}catch(e){setError(e instanceof Error?e.message:'로그인하지 못했습니다.');}finally{setBusy(false);}}
 return <main className="auth-page"><section className="auth-card"><img src="/ganbyeong24-logo-cropped.webp" alt="간병24" width="130"/><h1>간병24 로그인</h1><p>승인된 계정으로 업무를 시작하세요.</p><form onSubmit={submit}><label>아이디<input name="username" autoComplete="username" required minLength={4} maxLength={32}/></label><label>비밀번호<input name="password" type="password" autoComplete="current-password" required minLength={12} maxLength={128}/></label><p role="alert">{error}</p><button disabled={busy}>{busy?'확인 중…':'로그인'}</button></form><a href="/signup">계정이 없으신가요? 가입 신청</a><p>승인 상태나 계정 이용에 문제가 있으면 간병24 관리자에게 문의해 주세요.</p></section></main>;
}
